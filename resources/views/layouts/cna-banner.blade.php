@guest
    <div class="join-banner">
        <div class="join-banner-inner">
            <div>
                <h2>Ready to <em>shop?</em></h2>
                <p>Create a free account to unlock purchasing, track your orders, and save your favourites.</p>
            </div>
            <div class="join-actions">
                <a href="{{ route('register') }}" class="btn btn-gold">Create free account</a>
                <a href="{{ route('login') }}" class="btn btn-outline-white">Sign in</a>
            </div>
        </div>
    </div>
@endguest
